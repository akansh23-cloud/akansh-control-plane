export type NodeKind='primary'|'service'|'gate'|'store'|'control';export type EdgeKind='flow'|'pull'|'provision';export type DiagramNode={id:string;label:string;detail:string;col:number;row:number;kind:NodeKind};export type DiagramEdge={from:string;to:string;kind:EdgeKind;label?:string};export type Project={id:string;index:string;title:string;short:string;type:string;tagline:string;stack:string[];problem:string;role:string;architecture:string;approach:string[];outcome:string[];principle:string;lanes:{row:number;label:string}[];nodes:DiagramNode[];edges:DiagramEdge[];migration?:string[]};
export const projects:Project[]=[{id:'map',index:'01',title:'Migration Assurance Platform',short:'MAP',type:'Personal DevOps Project',tagline:'Fail-fast, audit-ready cutovers.',stack:['AWS','Amazon EKS','Amazon ECR','Application Load Balancer','Terraform','Argo CD','Docker','GitLab CI/CD'],problem:'A migration is usually declared complete without a repeatable record of whether the target matches the source. The platform makes that check an explicit state a release has to reach before cutover.',role:'Designed the deployment architecture and built the release and verification path.',architecture:'Terraform provisions the AWS infrastructure. GitLab CI/CD builds, tests and scans, then publishes an immutable image to Amazon ECR. Deployment configuration lives in Git and Argo CD reconciles it into Amazon EKS, which pulls the image from ECR. At runtime an Application Load Balancer fronts the workloads.',approach:['Designed AWS deployment architecture using Terraform-provisioned infrastructure.','Designed workloads around Amazon EKS behind an Application Load Balancer.','Used Amazon ECR for immutable image promotion across environments.','Built GitLab CI/CD release orchestration.','Used GitOps-based continuous delivery with Argo CD.','Included automated migration verification with pre-cutover quality states.'],outcome:['Pre-cutover quality is an explicit PASS / WARN / FAIL state rather than a judgement call.','Multi-role sign-off concepts let a release fail fast and leave an audit trail.','Promotion is immutable — the artifact that passed is the artifact that ships.'],principle:'Verification changes behaviour only when it is a gate. The same evidence produced after a cutover is a report, not a control.',lanes:[{row:0,label:'Build'},{row:1,label:'GitOps'},{row:2,label:'Runtime'},{row:3,label:'Infrastructure'}],nodes:[{id:'git',label:'Git',detail:'Application source and merge requests.',col:0,row:0,kind:'control'},{id:'gitlab',label:'GitLab CI/CD',detail:'Release orchestration: build, test and publish.',col:1,row:0,kind:'primary'},{id:'gate',label:'Build · Test · Scan',detail:'Compilation, tests and security scanning before an image is allowed out.',col:2,row:0,kind:'gate'},{id:'image',label:'Container image',detail:'Immutable image, tagged once and never rebuilt per environment.',col:3,row:0,kind:'store'},{id:'ecr',label:'Amazon ECR',detail:'Registry holding the promoted image. EKS pulls from here at deploy time.',col:4,row:0,kind:'store'},{id:'config',label:'Deployment config',detail:'Desired state in Git — the source of truth Argo CD reconciles against.',col:2,row:1,kind:'control'},{id:'argo',label:'Argo CD',detail:'Reconciles the declared state into the cluster. It does not build or push images.',col:3,row:1,kind:'primary'},{id:'user',label:'User',detail:'Request traffic entering the platform.',col:0,row:2,kind:'control'},{id:'alb',label:'ALB',detail:'Application Load Balancer terminating and routing requests.',col:1,row:2,kind:'primary'},{id:'eks',label:'Amazon EKS',detail:'Runs the workloads and pulls application images from ECR.',col:3,row:2,kind:'primary'},{id:'svc',label:'Services / Pods',detail:'Verification, reporting and sign-off services.',col:4,row:2,kind:'service'},{id:'tf',label:'Terraform',detail:'Provisions the infrastructure lifecycle — network, cluster, registry, load balancer. It is not in the request path.',col:1,row:3,kind:'control'}],edges:[{from:'git',to:'gitlab',kind:'flow'},{from:'gitlab',to:'gate',kind:'flow'},{from:'gate',to:'image',kind:'flow'},{from:'image',to:'ecr',kind:'flow',label:'push'},{from:'config',to:'argo',kind:'flow'},{from:'argo',to:'eks',kind:'flow',label:'reconcile'},{from:'user',to:'alb',kind:'flow'},{from:'alb',to:'eks',kind:'flow'},{from:'eks',to:'svc',kind:'flow'},{from:'eks',to:'ecr',kind:'pull',label:'image pull'},{from:'tf',to:'alb',kind:'provision'},{from:'tf',to:'eks',kind:'provision'},{from:'tf',to:'ecr',kind:'provision'}]},{id:'career-autopilot',index:'02',title:'Career Autopilot',short:'AUTOPILOT',type:'Personal DevOps Project',tagline:'A monolith taken apart one service at a time.',stack:['Docker','CI/CD Pipelines','API Gateway','Monorepo','Vercel'],problem:'A working monolith had to become sixteen independently deployable services, extracted one at a time rather than replaced in a single cutover.',role:'Owned deployment, containerization and the migration path.',architecture:'A monorepo holds the services, each with its own multi-stage Dockerfile and pipeline path; path-based change detection means only the affected service builds. The front end is hosted on Vercel and calls an API gateway, whose routing layer sends each path either to an extracted service or back to the monolith.',approach:['Owned deployment and containerization for a 16-service microservices architecture.','Created per-service Dockerfiles using multi-stage builds.','Managed incremental service-by-service migration from a legacy monolith.','Built monorepo CI/CD pipelines with per-service build, test and deploy stages plus path-based change detection.','Configured API gateway routing with automatic fallback to maintain service availability throughout migration.'],outcome:['Each service builds and ships on its own, triggered only by the paths that changed.','Gateway fallback keeps a path served by the monolith until its service is live.','Multi-stage builds keep the per-service images small.'],principle:'In an incremental migration, routing and fallback are first-class controls: every extracted path needs a safe route back until the new service is verified.',lanes:[{row:0,label:'Delivery'},{row:1,label:'Request path'},{row:2,label:'Fallback'}],nodes:[{id:'repo',label:'Monorepo',detail:'All services in one repository, with per-service Dockerfiles.',col:1,row:0,kind:'control'},{id:'ci',label:'Per-service CI/CD',detail:'Path-based change detection runs build, test and deploy only for what changed.',col:2,row:0,kind:'primary'},{id:'user',label:'User',detail:'Client requests.',col:0,row:1,kind:'control'},{id:'fe',label:'Vercel frontend',detail:'Hosted front end — the only public entry point.',col:1,row:1,kind:'primary'},{id:'gw',label:'API Gateway',detail:'Single API origin for the client, regardless of what is behind it.',col:2,row:1,kind:'primary'},{id:'routing',label:'Routing + fallback',detail:'Per-path routing rules with automatic fallback while a service is being extracted.',col:3,row:1,kind:'gate'},{id:'services',label:'16 services',detail:'Extracted services, independently built and deployed.',col:4,row:1,kind:'service'},{id:'mono',label:'Legacy monolith',detail:'Still serves every path not yet extracted.',col:4,row:2,kind:'store'}],edges:[{from:'repo',to:'ci',kind:'flow'},{from:'ci',to:'services',kind:'flow',label:'deploy'},{from:'ci',to:'fe',kind:'flow',label:'deploy'},{from:'user',to:'fe',kind:'flow'},{from:'fe',to:'gw',kind:'flow'},{from:'gw',to:'routing',kind:'flow'},{from:'routing',to:'services',kind:'flow'},{from:'routing',to:'mono',kind:'flow',label:'fallback'}],migration:['Monolith','Extract service','Route traffic','Verify','Repeat','16 services']}];
