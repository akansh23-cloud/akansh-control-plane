import { profile } from '@/data/profile';
export default function Footer() {
  const year = new Date().getFullYear();
  return <footer className="border-t border-line"><div className="shell flex flex-col gap-6 py-10 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-mono text-xs tracking-[0.22em] text-chalk">AKANSH <span className="text-mist">{'//'}</span> CONTROL PLANE</p><p className="mono-sm mt-2 text-mist">Built with engineering discipline.</p></div><div className="flex flex-wrap items-center gap-x-6 gap-y-2"><a className="mono-sm text-mist hover:text-chalk" href={`mailto:${profile.email}`}>Email</a><a className="mono-sm text-mist hover:text-chalk" href={profile.linkedin} target="_blank" rel="noopener noreferrer">LinkedIn</a><a className="mono-sm text-mist hover:text-chalk" href={profile.github} target="_blank" rel="noopener noreferrer">GitHub</a></div><p className="mono-sm text-mist"><span className="mr-2 inline-block h-1.5 w-1.5 rounded-full bg-pulse align-middle" />All systems nominal<span className="ml-3 text-line">|</span><span className="ml-3">{year}</span></p></div><p className="shell pb-8 text-[0.625rem] leading-relaxed text-mist/60">Status indicators, telemetry panels and the release simulation on this site are interface elements. They do not report real infrastructure.</p></footer>;
}
