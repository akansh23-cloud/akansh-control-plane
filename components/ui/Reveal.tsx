'use client';
import { motion } from 'framer-motion';import { useReducedMotion } from '@/hooks/useReducedMotion';import { useMode } from '@/components/providers/ModeProvider';
type RevealProps={children:React.ReactNode;delay?:number;variant?:'mask'|'lift';className?:string};
export default function Reveal({children,delay=0,variant='lift',className}:RevealProps){const reduced=useReducedMotion();const{mode}=useMode();const quiet=reduced||mode==='recruiter';if(quiet)return <div className={className}>{children}</div>;if(variant==='mask')return <motion.div className={className} initial={{clipPath:'inset(0 0 100% 0)',opacity:0}} whileInView={{clipPath:'inset(0 0 0% 0)',opacity:1}} viewport={{once:true,margin:'-10% 0px'}} transition={{duration:.9,ease:[.16,1,.3,1],delay}}>{children}</motion.div>;return <motion.div className={className} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-10% 0px'}} transition={{duration:.6,ease:[.16,1,.3,1],delay}}>{children}</motion.div>}
