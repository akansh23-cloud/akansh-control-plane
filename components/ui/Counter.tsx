'use client';
import { useEffect,useState } from 'react';import { useInView } from '@/hooks/useInView';import { useReducedMotion } from '@/hooks/useReducedMotion';
export default function Counter({value,className}:{value:string;className?:string}){const{ref,inView}=useInView<HTMLSpanElement>();const reduced=useReducedMotion();const numeric=parseInt(value.replace(/\D/g,''),10)||0;const suffix=value.replace(/[0-9]/g,'');const[shown,setShown]=useState(reduced?numeric:0);useEffect(()=>{if(!inView||reduced){if(reduced)setShown(numeric);return;}const start=performance.now();const duration=900;let frame=0;const tick=(now:number)=>{const t=Math.min((now-start)/duration,1);const eased=1-Math.pow(1-t,3);setShown(Math.round(numeric*eased));if(t<1)frame=requestAnimationFrame(tick);};frame=requestAnimationFrame(tick);return()=>cancelAnimationFrame(frame);},[inView,numeric,reduced]);return <span ref={ref} className={className}>{shown}{suffix}</span>}
