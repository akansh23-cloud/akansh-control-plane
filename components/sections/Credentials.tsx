import { certifications,education,training } from '@/data/profile';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';
export default function Credentials(){return <Section id="credentials" index="10" eyebrow="Education & certifications"><div className="grid gap-12 lg:grid-cols-2"><Reveal><h2 className="display-lg">Education</h2><div className="mt-8 border-t border-line pt-6"><p className="font-display text-2xl tracking-tight">{education.school}</p><p className="mono-sm mt-2 text-mist">{education.place}</p><div className="rule my-5"/><p className="text-chalk">{education.degree}</p><p className="mono-sm mt-1 text-mist">{education.specialisation}</p><p className="label mt-4">{education.period}</p></div></Reveal><Reveal delay={.08}><h2 className="display-lg">Certifications</h2><ul className="mt-8 divide-y divide-line border-t border-line">{certifications.map(cert=><li key={cert.code} className="flex items-baseline justify-between gap-4 py-5"><div><p className="font-display text-xl tracking-tight">{cert.name}</p><p className="mono-sm mt-1 text-mist">{cert.issuer}</p></div><span className="label text-ion">{cert.code}</span></li>)}</ul><p className="label mt-8">Training</p><ul className="mt-3 space-y-1">{training.map(item=><li key={item.name} className="mono-sm text-mist">{item.name} <span className="text-line">·</span> {item.note}</li>)}</ul></Reveal></div></Section>}
