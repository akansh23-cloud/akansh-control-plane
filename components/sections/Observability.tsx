import { pillars,reliability } from '@/data/system';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';
export default function Observability(){return <Section id="observability" index="09" eyebrow="Observability & reliability"><Reveal variant="mask"><h2 className="display-lg">A release is not done<span className="block text-mist">when deployment succeeds.</span></h2></Reveal><p className="lede mt-6">SRE-aligned engineering practices connect rollout health, telemetry and recovery without claiming an SRE job title.</p><div className="mt-12 grid gap-px overflow-hidden border border-line bg-line lg:grid-cols-3">{pillars.map((p,i)=><article key={p.id} className="bg-graphite p-7"><p className="label text-ion">0{i+1} // {p.title}</p><p className="mt-4 text-sm leading-relaxed text-mist">{p.note}</p><div className="mt-5 flex flex-wrap gap-2">{p.tools.map(t=><span key={t} className="border border-line px-2 py-1 font-mono text-[0.625rem] text-chalk">{t}</span>)}</div></article>)}</div><div className="mt-14 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{reliability.map(r=><article key={r.title} className="border-t border-line pt-5"><p className="font-display text-xl tracking-tight">{r.title}</p><p className="mono-sm mt-2 text-mist">{r.note}</p></article>)}</div></Section>}
