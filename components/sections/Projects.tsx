'use client';
import { useState } from 'react';import { projects } from '@/data/projects';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';import ArchitectureDiagram from '@/components/viz/ArchitectureDiagram';
export default function Projects(){const[open,setOpen]=useState(projects[0].id);return <Section id="projects" index="06" eyebrow="Case studies"><Reveal variant="mask"><h2 className="display-lg">Architecture stories<span className="block text-mist">that show the tradeoffs.</span></h2></Reveal><div className="mt-14 space-y-8">{projects.map(project=><article key={project.id} className="surface overflow-hidden"><button type="button" className="flex w-full flex-col gap-4 p-6 text-left sm:p-8 lg:flex-row lg:items-end lg:justify-between" onClick={()=>setOpen(open===project.id?'':project.id)} aria-expanded={open===project.id}><div><p className="label text-ion">{project.index} <span aria-hidden="true">·</span> {project.type}</p><h3 className="mt-3 font-display text-4xl tracking-tight sm:text-5xl">{project.title}</h3><p className="mono-sm mt-3 text-mist">{project.tagline}</p></div><div className="flex max-w-xl flex-wrap gap-2">{project.stack.map(t=><span key={t} className="border border-line px-2 py-1 font-mono text-[0.625rem] text-mist">{t}</span>)}</div></button>{open===project.id&&<div className="border-t border-line p-6 sm:p-8"><div className="grid gap-8 lg:grid-cols-3"><div><p className="label text-ion">Problem</p><p className="mt-3 text-sm leading-relaxed text-mist">{project.problem}</p></div><div><p className="label text-ion">Architecture</p><p className="mt-3 text-sm leading-relaxed text-mist">{project.architecture}</p></div><div><p className="label text-ion">Engineering principle</p><p className="mt-3 text-sm leading-relaxed text-mist">{project.principle}</p></div></div><div className="mt-10"><ArchitectureDiagram project={project}/></div>{project.migration&&<div className="mt-10"><p className="label text-ion">Monolith → microservices transformation</p><ol className="mt-5 grid gap-2 sm:grid-cols-3 lg:grid-cols-6">{project.migration.map((step,i)=><li key={step} className="relative border border-line bg-smoke p-4"><span className="label text-ion">{String(i+1).padStart(2,'0')}</span><p className="mt-2 font-display text-lg tracking-tight">{step}</p>{i<project.migration!.length-1&&<span className="absolute -right-2 top-1/2 z-10 hidden text-ion lg:block">→</span>}</li>)}</ol></div>}</div>}</article>)}</div></Section>}
