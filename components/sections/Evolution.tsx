'use client';
import { evolution } from '@/data/experience';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';
export default function Evolution(){return <Section id="evolution" index="04" eyebrow="Progression"><div className="grid gap-10 lg:grid-cols-12"><div className="lg:col-span-4"><Reveal variant="mask"><h2 className="display-lg">Capability<span className="block text-mist">evolution</span></h2></Reveal><Reveal delay={.08}><p className="lede mt-6 text-sm sm:text-base">Each layer builds on the one before it. Ordered by capability rather than by year — only the employment periods above carry dates.</p></Reveal></div><div className="lg:col-span-8"><ol>{evolution.map((step,i)=><Reveal key={step.n} delay={i*.06}><li className="group grid grid-cols-[3rem_1fr] gap-6 border-t border-line py-7 transition-colors duration-300 hover:bg-[rgba(108,123,255,0.035)] sm:grid-cols-[5rem_1fr]"><span className="label pt-2 text-mist transition-colors duration-300 group-hover:text-ion">{step.n}</span><div><div className="flex flex-wrap items-baseline gap-x-4 gap-y-1"><h3 className="font-display text-2xl tracking-tight sm:text-3xl">{step.phase}</h3><span className="label">{step.scope}</span></div><p className="mono-sm mt-2 max-w-2xl text-mist">{step.note}</p></div></li></Reveal>)}</ol></div></div></Section>}
