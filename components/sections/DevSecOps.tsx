'use client';
import { useState } from 'react';import { gates } from '@/data/system';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';
export default function DevSecOps(){const[active,setActive]=useState(0);return <Section id="security" index="08" eyebrow="DevSecOps"><Reveal variant="mask"><h2 className="display-lg">Security is a gate,<span className="block text-mist">not a final report.</span></h2></Reveal><p className="lede mt-6">The order matters: source checks happen before packaging; Trivy scans only after the container image exists; promotion follows the image-security gate.</p><ol className="mt-12 grid gap-3 md:grid-cols-7">{gates.map((gate,i)=><li key={gate.id}><button type="button" onClick={()=>setActive(i)} className={`surface h-full w-full p-4 text-left transition-all ${active===i?'border-ion ion-glow':''}`}><span className="label text-ion">{String(i+1).padStart(2,'0')}</span><p className="mt-3 font-display text-lg tracking-tight">{gate.label}</p><p className="mono-sm mt-2 text-mist">{gate.note}</p></button></li>)}</ol><div className="mt-6 flex items-center gap-3 font-mono text-xs text-mist"><span className="h-2 w-2 rounded-full bg-pulse"/><span>Current gate: {gates[active].label}</span><span className="text-line">→</span><span>{active<gates.length-1?gates[active+1].label:'Deployment eligible'}</span></div></Section>}
