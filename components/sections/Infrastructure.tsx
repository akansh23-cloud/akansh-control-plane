'use client';
import { useState } from 'react';import { layers,connections } from '@/data/system';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';import { cx } from '@/lib/utils';
export default function Infrastructure(){const[active,setActive]=useState('Kubernetes');const linked=new Set(connections[active]??[]);return <Section id="system" index="07" eyebrow="Infrastructure"><Reveal variant="mask"><h2 className="display-lg">Platform layers<span className="block text-mist">with relationships.</span></h2></Reveal><p className="lede mt-6">Not a logo cloud. Select a technology to see the systems it connects to in the delivery and runtime architecture.</p><div className="mt-12 grid gap-px overflow-hidden border border-line bg-line lg:grid-cols-7">{layers.map(layer=><div key={layer.id} className="bg-graphite p-5"><p className="label text-ion">{layer.title}</p><div className="mt-4 flex flex-col gap-2">{layer.items.map(item=>{const on=item===active,related=linked.has(item);return <button key={item} type="button" onClick={()=>setActive(item)} className={cx('border px-3 py-2 text-left font-mono text-xs transition-all',on?'border-pulse bg-pulse/10 text-chalk':related?'border-ion/50 text-ion':'border-line text-mist hover:text-chalk')}>{item}</button>})}</div></div>)}</div><div className="surface mt-5 p-5"><p className="label text-ion">Selected // {active}</p><p className="mono-sm mt-2 text-mist">{linked.size?`Connected to ${[...linked].join(' · ')}`:'Select another platform capability to inspect its relationships.'}</p></div></Section>}
