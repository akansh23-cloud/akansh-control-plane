'use client';
import { profile } from '@/data/profile';import Section from '@/components/ui/Section';import Reveal from '@/components/ui/Reveal';
export default function Identity(){return <Section id="identity" index="01" eyebrow="Identity"><div className="grid gap-12 lg:grid-cols-12"><div className="lg:col-span-7"><Reveal variant="mask"><h2 className="display-lg">Release delivery<span className="block text-mist">for 50+ services.</span></h2></Reveal><Reveal delay={.08}><p className="lede mt-8">{profile.summary}</p></Reveal><Reveal delay={.14}><p className="lede mt-5">The work sits between a merge request and a healthy pod: pipelines that build and scan, Helm charts that describe an environment honestly, secrets injected rather than baked in, and probes that decide whether a rollout continues. In a regulated bank, the interesting constraint is not speed — it is being able to show what shipped, and why it was allowed to.</p></Reveal></div><div className="lg:col-span-5"><Reveal delay={.1}><div className="surface p-6"><p className="label">Primary capabilities</p><ul className="mt-4 grid gap-x-6 gap-y-2 sm:grid-cols-2 lg:grid-cols-1 xl:grid-cols-2">{profile.primaryCapabilities.map(item=><li key={item} className="mono-sm flex items-center gap-2 text-chalk"><span className="h-1 w-1 rounded-full bg-ion" aria-hidden="true"/>{item}</li>)}</ul><div className="rule my-6"/><dl className="grid grid-cols-2 gap-4"><div><dt className="label">Based in</dt><dd className="mt-1 font-display text-xl">Pune, India</dd></div><div><dt className="label">Experience</dt><dd className="mt-1 font-display text-xl">3+ years</dd></div><div><dt className="label">Current</dt><dd className="mt-1 font-display text-xl">Barclays</dd></div><div><dt className="label">Domain</dt><dd className="mt-1 font-display text-xl">BFSI platform</dd></div></dl></div></Reveal></div></div></Section>}
